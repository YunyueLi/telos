# 复制本文件为 web/.env.local（已被 .gitignore 忽略）。
# 全部可选——一项都不填也能跑：账号/同步降级为「本地优先」，倒推走本地 serve.py(:8787)。

# ── 倒推端点（前端连哪个后端）──
# 本地默认连 http://127.0.0.1:8787/derive（start.sh 起的 serve.py）；也可直接在 App「倒推」页底部粘 URL。
# 想连你部署的 Cloudflare Worker：填它的 /derive 地址。
# 生产环境由 Cloudflare Pages 环境变量注入，官方实例也有构建脚本默认值。
# NEXT_PUBLIC_TELOS_DERIVE_URL=https://telos-derive.<你的子域>.workers.dev/derive

# ── 账号 + 跨设备同步（可选；不填则所有学习数据只存在本地浏览器，功能不受影响）──
# 建一个 Supabase 项目后填，详见 SUPABASE.md。
# anon / publishable key 是前端公开配置（受行级安全 RLS 保护），不可替代服务端密钥。
# 绝不要用 sb_secret_… / service_role。
# NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=SUPABASE_PUBLISHABLE_KEY_REDACTED
