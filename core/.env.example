# 复制本文件为 core/.env，填入你自己的 key。
# core/.env 已被 .gitignore 忽略，不会进版本库；切勿把真实 key 提交或贴到任何公开处。
# 若旧 key 曾经泄露，请先到控制台作废、重新生成。

# 拿 key：DeepSeek 注册即用——无需信用卡、约 5M 免费 token（够倒推/微课玩很久），
# 价格也只有前沿模型的零头。到 https://platform.deepseek.com → API Keys 复制（sk-...）。
TELOS_LLM_API_KEY=sk-your-own-key-here
TELOS_LLM_BASE_URL=https://api.deepseek.com
TELOS_LLM_MODEL=deepseek-chat

# ── 可选：联网检索（让微课的「延伸学习」给出真实可点的链接，而非平台搜索页）──
# 不配也能正常用：自动降级回平台搜索链接。配上后微课会先联网搜真实来源，
# 再让模型只从真实结果里挑、绝不编造 URL（类似 Perplexity 的出处卡片）。
#   tavily : 专为 LLM 的检索 API，约 1000 次/月免费，到 https://tavily.com 注册拿 key（tvly-...）
#   youtube: YouTube Data API v3，到 Google Cloud Console 开通后拿 key（只返回真实视频）
TELOS_SEARCH_PROVIDER=none
TELOS_SEARCH_API_KEY=
